
2. Build the Frontend: 
   - Create a basic HTML/CSS layout with a form for login and a dropdown for selecting time zones. 
   - Use Moment.js to display the current time and convert time zones based on user selection. 
   - Implement authentication using JWT to store and use the token to access the API.API. 
   - Use Webpack to bundle the JavaScript files and Babel for transpiling. 


3. Build the Backend: 
   - Set up an Express server with routes for login and time conversion. 
   - Implement JWT authentication to protect the /convert-time route. 
   - Create logic for user authentication (use a simple hardcoded user for testing) and JWT token generation.














id's - passwords

NateS123 - chromeguard456

Texas987 - Longhornstate12

SportsSquad - Weball1995









 {
   "name": "npm-project"
   "version": "1.0.0"
   "main": "moment.js"
   "scripts": 
   {
     "test": "echo \"Error: no test specified\" && exit 1"
   },
   "author": "",
   "license": "ISC",
   "description": "",
   "dependencies": {
     "axios": "^1.7.7",
     "moment": "^2.30.1"
   }
}
